import 'package:flutter/material.dart';

class UnreadWidget extends StatelessWidget {
  const UnreadWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Unread"));
  }
}